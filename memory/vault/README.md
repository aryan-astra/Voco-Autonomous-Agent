# Vault placeholders

This directory stores local runtime vault data and safe tracked templates.

- Runtime files are ignored by Git because they can contain sensitive data.
- Use the `*.template.*` files as starter content and copy them to local runtime files when needed.
